import React from 'react';
import { Link } from 'react-router-dom';
import firebase from 'firebase';

class ChatTextBox extends React.Component {
  constructor() {
    super();
    this.state = {};
  }

  render() {
    return <main>new text box</main>;
  }
}

export default ChatTextBox;
